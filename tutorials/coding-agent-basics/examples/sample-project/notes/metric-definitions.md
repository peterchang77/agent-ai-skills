# Metric Definitions

## Completed response

A response whose `status` is exactly `complete`.

## Missing score

A response whose `score` field is empty. Missing scores are reported, not replaced or removed.

## Duplicate respondent ID

More than one row with the same `respondent_id`. Duplicates are flagged for review; this example does not decide which row is authoritative.
