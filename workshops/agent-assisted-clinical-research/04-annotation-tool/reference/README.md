# Optional finished example

[`review-tool.html`](review-tool.html) is a complete local-only reference implementation for exercise 04. Open it only after you have attempted the exercise or if you need to recover. It is intentionally a single portable file with no external dependencies or network requests.

Use fabricated data when browsing the repository. Load real report-derived content only in an approved workspace and handle exported annotations as protected artifacts.
